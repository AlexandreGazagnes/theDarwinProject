

1 FIX UUID for each Algo session
  --> various session use the same global Algo
  --> plz use ALGO as a dict, each new instance add an isntance of the dict {"id" : object}
  --> be sure that with several various browser opened in // unique object 
  --> Why not useing a IO pk or REDIS cash manager ? because Now we use ALGO dict of object in ... RAM --> NOT SO GOOD


2 / Add various new charts xsys, learning curve etc etc